// import ChatBox from "@/components/chat/ChatBox";

// export default function Page({ params }) {
//   const user = { id: params.userId }; // replace with session

//   return <ChatBox projectId={params.projectId} user={user} />;
// }